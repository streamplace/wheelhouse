import React from "react";
import "./Dropdown.css";

const Dropdown = (props) => {
  return (
    <select className="pod-action-dropdown">
      <option value="delete">Delete</option>
    </select>
  );
};

export default Dropdown;
